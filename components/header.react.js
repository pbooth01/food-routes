/** @jsx React.DOM */

var React = require('react');

module.exports = Header = React.createClass({

  // Render the component
  render: function(){
    return (
      <header>
      	<h1> Food Routes! </h1>
      </header>
    )
  }

});