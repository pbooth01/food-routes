/** @jsx React.DOM */

var React = require('react');

// Export the TweetsApp component
module.exports = Application = React.createClass({

  // Render the component
  render: function(){
    return (
      <div className="routes-app">My first react application</div>
    )
  }

});
